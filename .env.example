# Copia este archivo a ".env" (sin el ".example") y pon contraseñas reales.
# El archivo ".env" no se sube a GitHub (ya está en .gitignore).

POSTGRES_USER=capexopex_admin
POSTGRES_PASSWORD=cambia-esto-por-una-contrasena-fuerte
POSTGRES_DB=proyectos_db

RABBITMQ_USER=capexopex_rabbit
RABBITMQ_PASSWORD=cambia-esto-tambien